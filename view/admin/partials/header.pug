header.header 
    .container-admin 
        .row.align-item-center 
            .col-3 
                .inner-logo
                   a(href="/admin/deshboard")
                      img(src='/images/logo.png' alt="logo")
            .col-9
                div #{record}
                div(class="text-right")
                    a(href=`${prefixAdmin}/auth/logout` class="btn btn-danger btn-sm") Đăng xuất
            